apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payroll-calculator.fullname" . }}-test-connection"
  labels:
    {{- include "payroll-calculator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payroll-calculator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
